// constants.js

const TICK_RATE = 50;
const DELTA_TIME = 1 / TICK_RATE;
const STEP = TICK_RATE / 1000;
const PLAYER_SPEED = 160;
const PLAYER_RADIUS = 15;

module.exports = {
  TICK_RATE,
  STEP,
  DELTA_TIME,
  PLAYER_SPEED,
  PLAYER_RADIUS
};